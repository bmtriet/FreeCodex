# Experiments

This directory is for small prototypes and learning loops.

Each experiment should be bounded and public-safe. Prefer a short brief before building and a concise result note afterward.

Use `templates/experiment-spec.md` for new experiment briefs.

